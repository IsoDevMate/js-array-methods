// used when we need to iterate and return data for each element of an array
//call the function for each element
let lengths = ["Bilbo", "Gandalf", "Nazgul"].map(item => item.length);
alert(lengths); // 5,7,6