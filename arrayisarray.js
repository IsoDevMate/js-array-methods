/*But arrays are used so often that there’s a special method for that:
 Array.isArray(value). It returns true if the value is an array,
  and false otherwise. */

console.log(Array.isArray({})); // false
console.log(Array.isArray([])); // true